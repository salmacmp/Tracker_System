#pragma once
class GUI
{
public:
	GUI(void);
	~GUI(void);
	// call initilizer with relative bool (restart)(on -off )options 
	// call initilizer add something to database [maybe image or capture image online from camera  ] or remove
	//call zoom module to get output and show on some image window 
	// direct the user that at the begining (after start button is pressed) that the user freez for sometime on the for example the first 2 seconds and then allow the user to move as he wish.
	// momken nzwed aw n4el y3ne e4ta :D
};

